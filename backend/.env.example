# Certify Intel - Environment Configuration
# Copy this file to .env and fill in your values
#
# STATUS: Phase 2 - Free/Open Source APIs Only
# All required paid API keys (Crunchbase, PitchBook, LinkedIn, SimilarWeb) have been removed.
# The system works without any API keys - optional keys enhance features.

# =============================================================================
# ✅ REQUIRED CONFIGURATION
# =============================================================================
# These are minimally needed for the app to start

SECRET_KEY=your-secret-key-here-use-a-random-string-at-least-32-chars

# =============================================================================
# ✅ CORE DATA COLLECTION (Free/Open Source - No API Keys Needed)
# =============================================================================
# These scrapers work out of the box with NO API keys required

# 1. Playwright Web Scraper - Extracts website content
#    (Requires: pip install playwright && playwright install chromium)
# Status: ✅ Working

# 2. SEC Edgar (via yfinance) - Public company financials (Free API)
#    (Requires: pip install yfinance pandas)
# Status: ✅ Working

# 3. Google News RSS - Real-time news monitoring (Free)
#    (Requires: stdlib only)
# Status: ✅ Working

# 4. Known Data Fallback - Pre-populated data for 15+ sources
#    (Requires: nothing, built-in)
# Status: ✅ Working

# =============================================================================
# 🔧 OPTIONAL: AI Features - OpenAI (GPT-4)
# =============================================================================
# Enables: Executive summaries, discovery agent, web extraction, chat
# If not set: AI features disabled, other features work normally
# Get key: https://platform.openai.com/api-keys

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1

# =============================================================================
# 🔧 OPTIONAL: AI Features - Google Gemini (v5.0.2+)
# =============================================================================
# Enables: Cost-effective AI (~90% cheaper than OpenAI for bulk tasks)
# Get key: https://aistudio.google.com/app/apikey
# Documentation: https://ai.google.dev/gemini-api/docs
#
# Recommended Models:
# - gemini-2.5-flash: Fast, cost-effective ($0.075/1M tokens)
# - gemini-2.5-flash-lite: Cheapest for bulk operations ($0.019/1M tokens)
# - gemini-2.5-pro: Highest quality for complex analysis ($1.25/1M tokens)

GOOGLE_AI_API_KEY=
GOOGLE_AI_MODEL=gemini-2.5-flash

# =============================================================================
# 🔧 AI PROVIDER CONFIGURATION (v5.0.2+)
# =============================================================================
# Controls which AI provider to use for different tasks
#
# AI_PROVIDER options:
# - "openai": Use OpenAI for all tasks
# - "gemini": Use Gemini for all tasks
# - "hybrid": Smart routing - Gemini for bulk, OpenAI for quality (RECOMMENDED)

AI_PROVIDER=hybrid

# Task-specific routing (only used when AI_PROVIDER=hybrid)
# AI_BULK_TASKS: Provider for high-volume operations (extraction, news)
# AI_QUALITY_TASKS: Provider for quality-critical tasks (summaries)
AI_BULK_TASKS=gemini
AI_QUALITY_TASKS=openai

# Fallback: Auto-switch providers on failure/rate-limit
AI_FALLBACK_ENABLED=true

# =============================================================================
# 🔧 OPTIONAL: Vertex AI (v5.3.0 - Enterprise AI)
# =============================================================================
# Enables: Enterprise-grade AI with RAG, Vector Search, Agent Builder, HIPAA
# Requires: GCP project with Vertex AI API enabled
# Documentation: https://cloud.google.com/vertex-ai/docs
#
# Prerequisites:
# 1. Create GCP project: https://console.cloud.google.com
# 2. Enable Vertex AI API: https://console.cloud.google.com/apis/library/aiplatform.googleapis.com
# 3. Create Service Account with roles/aiplatform.user
# 4. Download JSON key and set GOOGLE_APPLICATION_CREDENTIALS
#    OR run: gcloud auth application-default login

# GCP Project (required for Vertex AI)
GOOGLE_CLOUD_PROJECT=
# or: GCP_PROJECT_ID=

# Vertex AI Location (default: us-central1)
VERTEX_AI_LOCATION=us-central1

# Vertex AI Model (default: gemini-2.5-flash)
VERTEX_AI_MODEL=gemini-2.5-flash

# Model routing for different task types
VERTEX_AI_SUMMARY_MODEL=gemini-2.5-pro
VERTEX_AI_EXTRACTION_MODEL=gemini-2.5-flash
VERTEX_AI_BULK_MODEL=gemini-2.5-flash-lite

# RAG Engine (Phase 2) - Per-competitor knowledge bases
VERTEX_AI_RAG_ENABLED=false
VERTEX_AI_RAG_PREFIX=certify_intel

# Vector Search (Phase 3) - Semantic competitor search
VERTEX_AI_VECTOR_SEARCH_ENABLED=false
VERTEX_AI_VECTOR_INDEX=

# Agent Builder (Phase 4) - Autonomous CI agents
VERTEX_AI_AGENT_ENABLED=false
VERTEX_AI_AGENT_ID=

# Security & Compliance
VERTEX_AI_AUDIT_LOGGING=true
VERTEX_AI_VPC_SC=false
VERTEX_AI_CMEK_KEY=

# Cost Management
VERTEX_AI_MONTHLY_BUDGET=100
VERTEX_AI_COST_ALERT_THRESHOLD=0.8

# Feature Flags
VERTEX_AI_GROUNDING=true
VERTEX_AI_MULTIMODAL=true
VERTEX_AI_STREAMING=true

# =============================================================================
# 🔧 OPTIONAL: Google Custom Search (Enhanced Discovery)
# =============================================================================
# Enables: Better competitor discovery via Google search
# Get API Key: https://console.cloud.google.com/apis/credentials
# Get Search Engine ID: https://programmablesearchengine.google.com/
GOOGLE_API_KEY=
GOOGLE_CX=

# =============================================================================
# 🔧 OPTIONAL: Email Alerts (Requires SMTP Server)
# =============================================================================
# Enables: Change notifications via email
# If not set: Alerts still logged, but not sent via email
#
# For Gmail:
# 1. Enable 2-Factor Authentication
# 2. Create App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated password below (NOT your Gmail password)

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Sender and recipients (comma-separated for multiple recipients)
ALERT_FROM_EMAIL=certify-intel@yourcompany.com
ALERT_TO_EMAILS=analyst@yourcompany.com

# =============================================================================
# 🔧 OPTIONAL: Slack Notifications
# =============================================================================
# Enables: Real-time alerts to Slack channels
# If not set: Notifications disabled
#
# Setup: Create incoming webhook in your Slack workspace
# https://api.slack.com/messaging/webhooks

SLACK_WEBHOOK_URL=

# =============================================================================
# 🔧 OPTIONAL: Microsoft Teams Notifications
# =============================================================================
# Enables: Real-time alerts to Teams channels
# If not set: Notifications disabled

TEAMS_WEBHOOK_URL=

# =============================================================================
# 🔧 OPTIONAL: SMS Alerts via Twilio
# =============================================================================
# Enables: SMS notifications for critical alerts
# If not set: SMS disabled
# Setup: https://www.twilio.com/console

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_TO_NUMBERS=

# =============================================================================
# 🔧 OPTIONAL: Enhanced News (NewsAPI)
# =============================================================================
# Enables: Extended news coverage beyond Google News RSS
# If not set: Uses free Google News only
# Get free key: https://newsapi.org (free tier: 100 requests/day)

NEWSAPI_KEY=

# =============================================================================
# 🔧 OPTIONAL: Additional News APIs (v5.0.4 - Phase 3)
# =============================================================================
# These free APIs enhance news coverage with more sources and better filtering.
# All have generous free tiers - no credit card required.

# GNews API - 100 requests/day free
# Features: 60,000+ sources, historical to 6 years, fast response
# Get free key: https://gnews.io (register free account)
GNEWS_API_KEY=

# MediaStack API - 500 requests/month free
# Features: 7,500+ sources in 50 countries, 13 languages
# Get free key: https://mediastack.com (register free account)
MEDIASTACK_API_KEY=

# NewsData.io API - 200 requests/day free
# Features: 89 languages, tech/healthcare specialty, 1M+ articles/week
# Get free key: https://newsdata.io (register free account, no credit card)
NEWSDATA_API_KEY=

# =============================================================================
# ⚙️ SERVER CONFIGURATION
# =============================================================================

HOST=0.0.0.0
PORT=8000
DEBUG=false

# =============================================================================
# 🖥️ DESKTOP MODE (Electron App)
# =============================================================================
# For desktop app deployment - bypasses login
DESKTOP_MODE=false
ADMIN_EMAIL=admin@yourcompany.com

# =============================================================================
# 📊 DATABASE
# =============================================================================
# SQLite (default, included):
DATABASE_URL=sqlite:///./certify_intel.db

# PostgreSQL (for production):
# DATABASE_URL=postgresql://user:password@localhost:5432/certify_intel

# =============================================================================
# 🤖 ML SENTIMENT ANALYSIS (v5.0.5 - Phase 4)
# =============================================================================
# Uses Hugging Face transformers for accurate sentiment classification
# Models are downloaded automatically on first use (~500MB)
#
# Available models:
# - general: distilbert-base-uncased-finetuned-sst-2-english (fast, general)
# - financial: ProsusAI/finbert (best for business news)
# - multilingual: bert-base-multilingual-uncased-sentiment
#
# Note: Requires pip install transformers torch accelerate

ML_SENTIMENT_MODEL=financial
ML_USE_GPU=false

# =============================================================================
# 🎬 MULTIMODAL AI (v5.0.6 - Phase 3)
# =============================================================================
# Gemini-powered analysis of images, PDFs, and videos
# Requires GOOGLE_AI_API_KEY to be set
#
# Screenshot analysis: Competitor website screenshots
# PDF analysis: Whitepapers, case studies, datasheets, annual reports
# Video analysis: Demo videos, webinars, tutorials
#
# Note: Video files limited to 50MB for API upload

# =============================================================================
# 🔍 REAL-TIME GROUNDING (v5.0.6)
# =============================================================================
# Uses Gemini's built-in Google Search for current information
# Automatically enabled when GOOGLE_AI_API_KEY is set
# Uses gemini-2.0-flash model with grounding

# =============================================================================
# 📰 BULK NEWS PROCESSING (v5.0.6)
# =============================================================================
# Process 100+ articles efficiently using Flash-Lite
# Analysis types: summary, sentiment, categorize, extract
# Cost: ~$0.05 for 1000 articles

# =============================================================================
# 🔥 FIRECRAWL WEB SCRAPING (v5.0.6 - NEWS-4D)
# =============================================================================
# Enhanced web scraping with JavaScript rendering and anti-bot bypass
# Features: Clean markdown extraction, structured data, bulk processing
# Get API key: https://www.firecrawl.dev/
# Pricing: Free tier (500 credits/month), Pro ($19/month)

FIRECRAWL_API_KEY=

# =============================================================================
# 🎯 DISCOVERY AGENT
# =============================================================================
# DuckDuckGo search is free but rate-limited (used by default)
# For production, optional enhanced search APIs:
# SERPAPI_KEY=
# BING_SEARCH_API_KEY=

# =============================================================================
# REMOVED (Paid APIs - No Longer Supported)
# =============================================================================
# The following paid APIs have been removed from this version:
#
# ❌ CRUNCHBASE_API_KEY - Subscription required (~$1,000+/month)
#    Use: Funding data (now uses known data fallback)
#
# ❌ PITCHBOOK_API_KEY - Enterprise only (~$5,000+/month)
#    Use: Valuation data (now uses known data fallback)
#
# ❌ LINKEDIN_API_KEY - Requires special access
#    Use: Employee data (now uses known data fallback)
#
# ❌ SIMILARWEB_API_KEY - Paid subscription
#    Use: Website traffic (not critical, can use known data)
#
# ❌ HUBSPOT_API_KEY - CRM integration (paid)
#    Use: Account data (not implemented)

# =============================================================================
# 📝 QUICK START
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your settings
# 3. MINIMUM: Only SECRET_KEY is truly required
# 4. RECOMMENDED: Add OPENAI_API_KEY and SMTP config for full features
# 5. Start: python main.py
#
# On startup, the app will show which features are available based on
# which environment variables are configured.
# =============================================================================
